/* ============================================================
   HOME — vacía a propósito. Solo Navbar + Footer (via layout.js).
   Esperando diseño.
   ============================================================ */

export default function Home() {
  return null;
}
